
<%= form_tag("/votes/update", id: "upvote", style: "text-align: center;", remote: true) do |f| %>

	<%= hidden_field_tag :voteable_id,	 object.id %>
	<%= hidden_field_tag :voteable_type, object.class.to_s %>
	<%= hidden_field_tag :vote_value,    1 %>

	<button type="submit" class="no-btn" title="This question shows research effort; it is useful and clear">
  	<span class="glyphicon glyphicon-triangle-top"></span>
	</button>

<% end %>