
<%= form_tag("/comments", remote: true) do |f| %>

	<%= text_field_tag "content-answer-#{answer.id}", nil, 
											class:       'comment-field', 
											placeholder: 'Add comment' %>

	<%= hidden_field_tag :answer_id, answer.id %>

<% end %>